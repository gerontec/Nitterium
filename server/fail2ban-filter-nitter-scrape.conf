# Tweet detail pages (/<user>/status/<id>) cost one X API call per request.
# Only excessive fetching from a single IP is caught; normal reading stays free.
[Definition]
failregex = ^<HOST> .*"(GET|HEAD|POST) [^"]*/status/[0-9]+
ignoreregex =
